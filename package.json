{
  "name": "job-heat",
  "version": "1.0.0",
  "description": "Determine demand of job title based on listings scrape",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ryan lacroix",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.22.0",
    "express": "^4.14.0",
    "indeed-jobs-api": "^0.1.1",
    "request": "^2.79.0"
  }
}
